apiVersion: apps/v1
kind: Deployment
metadata:
  name: deploy-invalid
spec:
  replicas: 1
  selector:
    matchLabels:
      app: demo
  template:
    metadata:
      labels:
        app: demo
    spec:
      containers:
      - name: nginx
        image: nginx:latest